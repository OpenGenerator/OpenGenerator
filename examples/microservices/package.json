{
  "name": "@opengenerator/example-microservices",
  "version": "0.0.1",
  "private": true,
  "description": "Microservices architecture example with OpenGenerator",
  "type": "module",
  "scripts": {
    "dev": "echo 'Run: cd services/gateway && pnpm dev, cd services/user && pnpm dev'",
    "build": "cd services/gateway && pnpm build && cd ../user && pnpm build",
    "start": "echo 'Run: cd services/gateway && pnpm start, cd services/user && pnpm start'",
    "test": "echo 'Run tests in each service directory'",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.6.0",
    "opengenerator": "workspace:*"
  }
}

/**
 * {{pascalCase name}} Types
 * Generated by OpenGenerator
 */

/**
 * {{pascalCase name}} entity
 */
export interface {{pascalCase name}} {
  id: string
{{#each fields}}
  {{name}}{{#unless required}}?{{/unless}}: {{type}}
{{/each}}
  createdAt: Date
  updatedAt: Date
{{#if softDelete}}
  deletedAt: Date | null
{{/if}}
}

/**
 * Create {{pascalCase name}} input
 */
export interface Create{{pascalCase name}}Input {
{{#each fields}}
{{#unless isGenerated}}
  {{name}}{{#unless required}}?{{/unless}}: {{type}}
{{/unless}}
{{/each}}
}

/**
 * Update {{pascalCase name}} input
 */
export interface Update{{pascalCase name}}Input {
{{#each fields}}
{{#unless isGenerated}}
  {{name}}?: {{type}}
{{/unless}}
{{/each}}
}

/**
 * {{pascalCase name}} filter options
 */
export interface {{pascalCase name}}Filter {
  search?: string
{{#each fields}}
{{#if filterable}}
  {{name}}?: {{type}}
{{/if}}
{{/each}}
{{#each dateFields}}
  {{name}}_from?: string
  {{name}}_to?: string
{{/each}}
}

/**
 * {{pascalCase name}} with relations
 */
export interface {{pascalCase name}}WithRelations extends {{pascalCase name}} {
{{#each relations}}
  {{name}}{{#unless required}}?{{/unless}}: {{type}}
{{/each}}
}
